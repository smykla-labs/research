[project]
name = "ocr-finder"
version = "1.0.0"
description = "Find text in images using EasyOCR, returning click coordinates"
requires-python = ">=3.11"
dependencies = []  # Managed by workspace root

[project.scripts]
ocr-finder = "ocr_finder.cli:main"

[tool.uv]
package = true
